import { HighlightCoursePipe } from './highlight-course.pipe';

describe('HighlightCoursePipe', () => {
  it('create an instance', () => {
    const pipe = new HighlightCoursePipe();
    expect(pipe).toBeTruthy();
  });
});
